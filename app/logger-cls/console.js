// /%[sdifjoO]/g
//util.format(format[, ...args])

//if first arg is string
//check for format
//format.match(/%[sdifjoO]/g).length

//if format and matching args, util.format
//and pass formatted string and extra args to logger
